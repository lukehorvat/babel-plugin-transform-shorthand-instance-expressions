{
  "private": true,
  "name": "babel-plugin-transform-shorthand-instance",
  "version": "0.0.0",
  "main": "dist",
  "scripts": {
    "build": "rimraf dist && babel lib -d dist",
    "prepublish": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "6.7.5",
    "babel-preset-es2015": "6.6.0",
    "rimraf": "2.5.2"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
